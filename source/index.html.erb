---
title: All talks
---

<% talks.group_by(&:date).each do |date, talks| %>
  <%= content_tag(:time, date.strftime('%-d %b %Y'), datetime: date.iso8601, class: 'grouping') %>
  
  <% talks.each do |talk| %>
    <%= partial "talks/overview", locals: { talk: talk } %>
  <% end %>
<% end %>
